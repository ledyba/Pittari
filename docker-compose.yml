---
version: '3.6'

services:
  web:
    image: pittari
    hostname: pittari_web
    build:
      context: ./
    container_name: pittari_web
    restart: always
    networks:
      - planet-link
    logging:
      driver: "json-file"
      options:
        max-file: '4'
        max-size: '250m'
networks:
  planet-link:
    external:
      name: planet-link
